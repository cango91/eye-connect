<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <% if(header && header.style){ %>
  <link rel="stylesheet" href="<%= header.style %>">
  <% } else { %>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <% } %>
  <% if(header && header.js){ %>
  <script <%- header.js.module ? `type="module"` : '' %> <%- header.js.defer ? `defer` : '' %> src="<%= header.js.file %>"> </script>
  <% } %>
  <title><%- header && header.title ? header.title : `eye Connect` %></title>
</head>

<body>